<div class="new-link-form">
  <h2>Edit Link</h2>
  <%= form_for(@link) do |f| %>
    <%= f.text_field :name, placeholder: link.name %>
    <%= f.text_field :url, placeholder: link.url %>
    <%= f.submit "Create Link" %>
  <% end %>
</div>
